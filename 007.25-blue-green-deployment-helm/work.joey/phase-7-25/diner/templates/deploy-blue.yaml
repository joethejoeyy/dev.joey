apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "diner.fullname" . }}-blue
  labels:
    app.kubernetes.io/name: {{ include "diner.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    color: blue
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "diner.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      color: blue
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "diner.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        color: blue
    spec:
      containers:
        - name: nginx
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html/index.html
              subPath: index.html
      volumes:
        - name: html
          configMap:
            name: {{ include "diner.fullname" . }}-blue-index
