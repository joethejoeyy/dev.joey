apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "diner-app.fullname" . }}-test-connection"
  labels:
    {{- include "diner-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "diner-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
