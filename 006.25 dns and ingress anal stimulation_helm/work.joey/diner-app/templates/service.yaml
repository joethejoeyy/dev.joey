apiVersion: v1
kind: Service
metadata:
  name: {{ include "diner-app.fullname" . }}
  labels:
    {{- include "diner-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diner-app.selectorLabels" . | nindent 4 }}
