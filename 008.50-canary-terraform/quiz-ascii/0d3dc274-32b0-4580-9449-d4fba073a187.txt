Terraform + Canary Overview
─────────────────────────────
┌─────────────┐
│ main.tf     │  → defines stable + canary Helm releases
└─────┬───────┘
      │
 terraform apply -var='canary_weight=10'
      │
      ▼
 stable (90%) ─────── canary (10%)   ← weighted routing
