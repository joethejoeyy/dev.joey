apiVersion: apps/v1
kind: Deployment
metadata:
  name: diner-stable
  namespace: diner-lab
spec:
  replicas: 3
  selector:
    matchLabels:
      app: diner
      version: stable
  template:
    metadata:
      labels:
        app: diner
        version: stable
    spec:
      containers:
      - name: nginx
        image: nginx:1.25
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html/index.html
          subPath: index.html
      volumes:
      - name: html
        configMap:
          name: diner-stable-html
