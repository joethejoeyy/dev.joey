apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "diner-stable.fullname" . }}-test-connection"
  labels:
    {{- include "diner-stable.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "diner-stable.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
