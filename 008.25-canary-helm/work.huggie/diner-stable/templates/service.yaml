apiVersion: v1
kind: Service
metadata:
  name: {{ include "diner-stable.fullname" . }}
  labels:
    {{- include "diner-stable.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diner-stable.selectorLabels" . | nindent 4 }}
