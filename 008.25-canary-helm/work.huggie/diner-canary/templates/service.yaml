apiVersion: v1
kind: Service
metadata:
  name: {{ include "diner-canary.fullname" . }}
  labels:
    {{- include "diner-canary.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diner-canary.selectorLabels" . | nindent 4 }}
